# =============================================================================
# Luqma Backend Environment Configuration
# =============================================================================
# Copy this file to .env and configure your environment variables
# NEVER commit the .env file to version control (it's in .gitignore)

# =============================================================================
# Spoonacular API Configuration (REQUIRED)
# =============================================================================
# Get your FREE API key from: https://spoonacular.com/food-api/console#Dashboard
# Free tier: 150 requests/day (sufficient for development and testing)
# Paid tiers: Available if you need higher quotas
SPOONACULAR_API_KEY=your_api_key_here

# =============================================================================
# Spring Profile (optional)
# =============================================================================
# Controls application behavior, logging, and configuration
# Options:
#   - dev:  Development mode (verbose logging, detailed errors, relaxed CORS)
#   - test: Testing mode (used during automated tests)
#   - prod: Production mode (optimized logging, generic errors, strict CORS)
# Default: dev
SPRING_PROFILES_ACTIVE=dev

# =============================================================================
# Common Configuration Scenarios
# =============================================================================

# Scenario 1: Local Development (DEFAULT)
# - Frontend: http://localhost:3000
# - Backend:  http://localhost:8080
# - Use: SPRING_PROFILES_ACTIVE=dev
# - Port configured in: backend/src/main/resources/application.yaml

# Scenario 2: Custom Backend Port
# - Edit backend/src/main/resources/application.yaml
# - Change: server.port value
# - Update frontend/.env: LUQMA_API_URL=http://localhost:YOUR_PORT/api/v1

# Scenario 3: Production Deployment
# - Use: SPRING_PROFILES_ACTIVE=prod
# - Configure production CORS origins in application-prod.yaml
# - Use HTTPS with valid SSL certificates
# - Set secure database credentials (if using database in future)
